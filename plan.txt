1. Implement the requested fixes in the Special Situation Arbitrage Dividends tool:
   - Make sure UnionBank appears by adding `symbols` back to `all_symbols` to prevent missing records when history is empty.
   - Use `set` when extracting F&O tickers to prevent duplicate symbol processing (fixes the Alkem repetition bug).
   - Round `expected_amount`, `last_amount`, and historical `amount` to the nearest 0.05 using `round(amount * 20) / 20`.
   - Update the UI to use XLS export (`exportSSDivXLS`) using `xlsx` instead of simple CSV construction, allowing full nested historical rows in a single sheet without destroying formatting.
2. Complete pre commit step: check testing.
3. Submit changes.
