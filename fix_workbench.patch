--- backend/ui/templates/workbench.html
+++ backend/ui/templates/workbench.html
@@ -3294,7 +3294,7 @@
                         }
                     }

-                    if (!hasLinkedAction && isPotentialMatch) {
+                    if (!hasLinkedAction && isDividendRelated) {
                         // Create a synthetic action

                         // Parse dividend amount if available (e.g., "Rs. 54 per share" or "54/-")
